INSERT INTO artist (
      name
    	)
      VALUES
      ('The Hypothetical 1'),
      ('The Hypothetical 2'),
      ('The Hypothetical 3');
      
      SELECT * FROM artist
      
      SELECT * FROM artist
      order by name desc 
      limit 10;
      
      SELECT * FROM artist
      order by name asc 
      limit 5;
      
      SELECT * FROM artist
      WHERE name like ('Black%');
      
      SELECT * FROM artist
      WHERE name like ('%Black%');
      